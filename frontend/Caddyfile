:80 {
    root * /usr/share/caddy

    handle /api* {
        reverse_proxy pinion-api:3000
    }
    handle {
        try_files {path} /index.html
        file_server
    }

    log {
        output stdout
    }
}
